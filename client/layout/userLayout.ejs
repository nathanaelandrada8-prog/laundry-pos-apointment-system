<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | CleanFast</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom styles for smooth transition and z-index */
        .sidebar {
            transition: width 0.3s ease-in-out;
        }
        .content-area {
            transition: margin-left 0.3s ease-in-out;
        }
        /* Styling for minimized links (only icon visible) */
        .sidebar.minimized .nav-link-text {
            display: none;
        }
        /* Add a style for the Indigo theme border in the footer of the nav */
        .border-indigo-600 {
            border-color: #4f46e5;
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen flex">

    <!-- SIDEBAR NAVIGATION -->
    <!-- Using bg-indigo-700 for the customer look -->
    <nav id="sidebar" class="sidebar w-64 bg-indigo-700 text-white flex flex-col shadow-xl h-screen fixed z-20">
        
        <!-- Header/Logo -->
        <div class="p-6 flex items-center justify-center border-b border-indigo-600">
            <span class="text-2xl font-bold tracking-wider whitespace-nowrap">
                <span id="logo-full">CleanFast</span>
                <span id="logo-icon" class="hidden">CF</span>
            </span>
        </div>

        <!-- Navigation Links -->
        <div class="flex-grow p-4 space-y-2">
            <!-- Dashboard -->
            <a href="/user/dashboard" class="nav-link flex items-center p-3 rounded-lg hover:bg-indigo-600 transition duration-150 group 
                <%= contentPartial === 'user/dashboard' ? 'bg-indigo-800' : '' %>">
                <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 01-1-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 01-1 1z"></path></svg>
                <span class="nav-link-text font-medium">Dashboard</span>
            </a>
            
            <!-- Schedule/Bookings -->
            <a href="/user/schedule" class="nav-link flex items-center p-3 rounded-lg hover:bg-indigo-600 transition duration-150 group
                <%= contentPartial === 'user/schedule' ? 'bg-indigo-800' : '' %>">
                <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
                <span class="nav-link-text font-medium">My Schedule</span>
            </a>
            
            <!-- Future link: Profile -->
            <a href="#" class="nav-link flex items-center p-3 rounded-lg hover:bg-indigo-600 transition duration-150 group">
                <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                <span class="nav-link-text font-medium">Profile</span>
            </a>
        </div>

        <!-- Footer/Logout -->
        <div class="p-4 border-t border-indigo-600">
            <!-- Changed to use onclick handler -->
            <a href="#" id="userLogoutLink" onclick="handleLogout(event)"
                class="nav-link flex items-center p-3 rounded-lg text-red-300 hover:bg-indigo-600 hover:text-red-100 transition duration-150 group">
                <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path></svg>
                <span class="nav-link-text font-medium">Logout</span>
            </a>
        </div>
    </nav>

    <!-- MAIN CONTENT AREA -->
    <div id="content-area" class="content-area flex-grow ml-64 min-h-screen flex flex-col">
        
        <!-- Top Bar with Toggle Button -->
        <header class="bg-white shadow-md p-4 sticky top-0 z-10">
            <div class="flex items-center justify-between">
                <button id="sidebar-toggle" class="text-gray-500 hover:text-indigo-600 transition duration-150">
                    <!-- Icon for Hamburger/Menu -->
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7"></path></svg>
                </button>
                <div class="text-gray-700">
                    <span class="font-medium">Welcome, <%= userName || 'User' %></span>
                </div>
            </div>
        </header>

        <!-- Dynamic Content Section -->
        <main class="flex-grow p-4 md:p-8">
            <%- include(contentPartial) %>
        </main>
    </div>

    <script src="../assets/js/user.js"></script>
    <script src="../assets/js/handleLogout.js"></script>
</body>
</html>